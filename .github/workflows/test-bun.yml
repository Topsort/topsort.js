name: Bun
on:
  pull_request:
    paths:
      - "**/*.ts"

jobs:
  bun-test:
    name: Test
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
      - run: bun install

      - name: Run tests
        run: bun test
