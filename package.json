{
  "name": "topsort.js",
  "private": true,
  "packageManager": "bun@1.3.3",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/web build && bun run --cwd packages/react-native build && bun run build:verification",
    "build:verification": "bun run --cwd packages/verification build",
    "typecheck": "bun run --cwd packages/core typecheck && bun run typecheck:verification",
    "typecheck:verification": "bun run --cwd packages/verification typecheck",
    "test": "bun run typecheck && bun run --cwd packages/web test && bun run --cwd packages/react-native test && bun run --cwd packages/react-native test:harness",
    "test:e2e": "bun run --cwd packages/web test:e2e",
    "format": "biome check",
    "format:fix": "biome check --write",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@playwright/test": "1.63.0",
    "@types/bun": "1.3.3",
    "bunup": "0.16.10",
    "lefthook": "2.1.14",
    "msw": "2.12.3",
    "typescript": "7.0.2"
  }
}
